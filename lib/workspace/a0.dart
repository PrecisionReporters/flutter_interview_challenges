import 'package:flutter/material.dart';

class ChallengeZeroWorkspace extends StatelessWidget {
  const ChallengeZeroWorkspace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Align(
        alignment: Alignment.topCenter,
        child: Text(
          'You will complete each challenge in this workspace.',
        ),
      ),
    );
  }
}
